{
  "framework": "nextjs",
  "installCommand": "npm ci --ignore-scripts && npx prisma generate --schema=packages/db/prisma/schema.prisma",
  "buildCommand": "npx turbo run build --filter=@steady/web",
  "outputDirectory": "apps/web/.next"
}
