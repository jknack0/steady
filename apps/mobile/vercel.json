{
  "buildCommand": "cd ../.. && npx turbo run build --filter=@steady/shared && cd apps/mobile && npx expo export --platform web",
  "installCommand": "cd ../.. && npm ci --ignore-scripts",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
